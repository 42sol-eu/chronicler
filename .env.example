# Jira Configuration
# Copy this file to ~/.env (your home directory) or to ./.env (project directory)
# The CLI will automatically load environment variables from either location

# Your Jira server URL
JIRA_SERVER_URL=https://stadlerrailag.atlassian.net

# Your Jira email address 
JIRA_EMAIL=your-email@company.com

# Your Jira API token
# Generate this from: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_API_TOKEN=your-api-token-here

# Installation Instructions:
# 1. Copy this file: cp .env.example ~/.env
# 2. Edit ~/.env with your actual values
# 3. The CLI will automatically load these variables

# Usage examples:
# 
# 1. Using environment variables (from ~/.env):
#    chronicler jira PPRX
#
# 2. Using command line options (overrides .env):
#    chronicler jira PPRX --server-url https://stadlerrailag.atlassian.net --email your-email@company.com --api-token your-token
#
# 3. Output formats:
#    chronicler jira PPRX --format table  # Rich table format (default)
#    chronicler jira PPRX --format json   # JSON format for scripting
